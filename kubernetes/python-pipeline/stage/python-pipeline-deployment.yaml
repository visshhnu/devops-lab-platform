apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-pipeline-app
  namespace: python-pipeline
  labels:
    app.kubernetes.io/name: python-pipeline-app
    environment: dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: python-pipeline-app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: python-pipeline-app
        environment: dev
    spec:
      containers:
        - name: python-pipeline-app
          image: docker.io/visshhnu/python-pipeline:stage
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
---
apiVersion: v1
kind: Service
metadata:
  name: python-pipeline-service
  namespace: python-pipeline
  labels:
    app.kubernetes.io/name: python-pipeline-service
    environment: dev
spec:
  selector:
    app.kubernetes.io/name: python-pipeline-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: python-pipeline-ingress
  namespace: python-pipeline
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - host: python.stage.devops-lab.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: python-pipeline-service
                port:
                  number: 80
